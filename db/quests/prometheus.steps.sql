-- =============================================================
-- «Прометей» — готовые уроки квеста в основной базе.
-- Пока добавлены этапы 1–5 из запланированных 24.
-- =============================================================

DELETE FROM quest_steps WHERE quest_slug = 'prometheus';

INSERT INTO quest_steps (
  quest_slug,
  step_number,
  title,
  story,
  outcome,
  theory,
  task,
  hint,
  expected_rows
) VALUES (
  'prometheus',
  1,
  'Поиск последнего критического события',
  $story$
**Краткая сводка**

- Инженер-механик Артём Вейл состыковался с грузовым кораблём «Прометей» через узел `DOCK-A`.
- Сразу после стыковки корабельная система отключила двигатели челнока и заблокировала механические захваты.
- Диспетчер не отвечает, а внутренняя дверь шлюза остаётся закрытой.
- Локальный технический журнал сохранился.
- Нужно найти последнее критическое событие в стыковочном секторе после прибытия Артёма.
$story$,
  $outcome$
**Автоматическая блокировка.**

Последнее критическое событие зарегистрировано в `03:18:42`. Система фиксации челнока активировала автоматическую блокировку и запретила ручное отсоединение.

Это не случайная механическая поломка. Сначала «Прометей» разрешил стыковку, выровнял давление и принял запрос на открытие внутренней двери. Затем корабельная система перехватила управление двигателями челнока и только после этого заблокировала его.

Причина команды пока неизвестна. Чтобы покинуть стыковочный сектор, Артёму придётся восстановить локальное питание и открыть внутреннюю дверь шлюза.
$outcome$,
  $theory$
Таблица `system_events` хранит журнал событий, но в ней нет названий систем и отсеков. Эти данные находятся в `ship_systems` и `sectors`, поэтому запрос должен соединить три таблицы.

Связи:

```
system_events.system_id → ship_systems.system_id
system_events.sector_id → sectors.sector_id
```

Соединение выполняется через `JOIN`:

```sql
JOIN ship_systems AS ss
  ON ss.system_id = se.system_id
```

`WHERE` оставляет только стыковочные сектора, события нужной критичности и записи после заданного времени:

```sql
WHERE s.sector_code = 'DOCK-A'
  AND ss.system_type = 'docking_control'
  AND se.severity >= 4
  AND se.recorded_at >= TIMESTAMP '2187-09-14 03:00:00'
```

Для поиска последней записи отсортируй события от новых к старым и оставь первую строку:

```sql
ORDER BY se.recorded_at DESC
LIMIT 1;
```
$theory$,
  'Найди последнее событие системы стыковки с критичностью не ниже 4, произошедшее в секторе DOCK-A после 2187-09-14 03:00:00. Тип системы должен быть docking_control. Выведи sector_code, sector_name, system_name, состояние системы под именем system_status, event_type, severity, event_message и recorded_at. Результат должен содержать одну самую позднюю запись.',
  'Начни с system_events AS se. Присоедини ship_systems AS ss по system_id и sectors AS s по sector_id. В WHERE укажи s.sector_code = ''DOCK-A'' и ss.system_type = ''docking_control'', затем используй ORDER BY se.recorded_at DESC и LIMIT 1.',
  '[
    {
      "sector_code":"DOCK-A",
      "sector_name":"Стыковочный узел A",
      "system_name":"Система фиксации челнока",
      "system_status":"damaged",
      "event_type":"AUTOMATIC_LOCK",
      "severity":5,
      "event_message":"Активирована автоматическая блокировка внешнего аппарата. Ручное отсоединение запрещено",
      "recorded_at":"2187-09-14 03:18:42"
    }
  ]'::jsonb
),
(
  'prometheus',
  2,
  'Определение последнего состояния систем',
  $story$
**Краткая сводка**

- Внутренняя дверь `DOCK-A` не получает питание.
- Аварийных батарей хватает только на освещение, датчики и локальную сеть.
- Рядом находится несколько энергетических узлов, но часть повреждена или недоступна.
- Для каждой системы нужно учитывать только её последнее событие: после отключения мог произойти перезапуск или новый сбой.
- Артёму нужен доступный распределитель, который был отключён вручную и допускает безопасный локальный запуск.
$story$,
  $outcome$
**Путь в инженерный отсек открыт.**

Главная инженерная силовая шина находится в `ENG-01`. Этот сектор напрямую связан с `DOCK-A` герметичным переходом. Дверь заблокирована, но требует третий уровень доступа — инженерный пропуск Артёма подходит.

Последнее событие шины — `MANUAL_SHUTDOWN`. Значит, питание исчезло не из-за короткого замыкания или разрушения оборудования: локальную блокировку установили намеренно.

Артём открывает сервисный проход, добирается до инженерного отсека и вручную снимает блокировку. Питание возвращается в `DOCK-A`, привод внутренней двери оживает, и путь внутрь «Прометея» открывается.
$outcome$,
  $theory$
`WITH` позволяет разделить сложный запрос на несколько понятных частей. В этом уроке первое общее табличное выражение формирует список достижимых секторов, а второе определяет последнее событие каждой системы.

```sql
WITH reachable_sectors AS (
  ...
),
ranked_events AS (
  ...
)
SELECT ...
```

Идентификатор `DOCK-A` можно получить подзапросом, не подставляя число вручную:

```sql
SELECT sector_id
FROM sectors
WHERE sector_code = 'DOCK-A'
```

`UNION` объединяет исходный сектор с соседними. Условия перехода проверяют состояние двери, уровень доступа и герметичность.

Чтобы сохранить всю последнюю запись, а не только её время, используй оконную функцию:

```sql
ROW_NUMBER() OVER (
  PARTITION BY se.system_id
  ORDER BY se.recorded_at DESC, se.event_id DESC
) AS event_rank
```

`PARTITION BY` начинает отдельную нумерацию для каждой системы. Строка с `event_rank = 1` является её последним событием. В отличие от `MAX(recorded_at)`, этот приём сохраняет тип события, сообщение и остальные колонки строки.
$theory$,
  'Найди энергетическую систему в DOCK-A или непосредственно связанном с ним секторе. Переход должен быть герметичным, иметь дверь open или locked и требовать уровень доступа не выше 3. Оставь системы типа power_distribution и только последнее событие каждой системы после 2187-09-14 02:30:00. Последнее событие должно иметь тип MANUAL_SHUTDOWN. Выведи sector_code, sector_name, door_status, system_name, состояние системы как system_status, priority_level, event_type, event_message и recorded_at.',
  'Собери reachable_sectors через UNION, затем в ranked_events вычисли ROW_NUMBER() с PARTITION BY system_id. В итоговом WHERE оставь event_rank = 1, system_type = ''power_distribution'' и event_type = ''MANUAL_SHUTDOWN''.',
  '[
    {
      "sector_code":"ENG-01",
      "sector_name":"Инженерный отсек",
      "door_status":"locked",
      "system_name":"Главная инженерная силовая шина",
      "system_status":"offline",
      "priority_level":5,
      "event_type":"MANUAL_SHUTDOWN",
      "event_message":"Подтверждена локальная блокировка главной силовой шины",
      "recorded_at":"2187-09-14 03:28:51"
    }
  ]'::jsonb
),
(
  'prometheus',
  3,
  'Группировка и поиск пропавшей смены',
  $story$
**Краткая сводка**

- После восстановления питания Артём вышел из шлюза на жилую палубу.
- В каютах остались личные вещи, но людей и следов организованной эвакуации нет.
- Расписание показывает, кто должен был отдыхать в жилых секторах в момент аварии.
- Нужно найти сектор, где все назначенные на отдых сотрудники числятся пропавшими.
- Если таких секторов несколько, нужен сектор с самой большой группой.
$story$,
  $outcome$
**Пустой корабль.**

В `HAB-01` должны находиться шесть отдыхающих членов экипажа. Все шестеро имеют статус `missing`. В остальных жилых секторах статусы сотрудников различаются, поэтому они не подходят под условие.

Исчезла не отдельная смена и не один случайный пассажир: целая группа покинула сектор, оставив одежду, кружки и включённые терминалы. Признаков обычной эвакуации нет.

Когда Артём начинает осмотр `HAB-01`, свет на несколько секунд гаснет. Из вентиляционной шахты доносится металлический скрежет, быстро уходит вдоль потолка и затихает за закрытой решёткой. Жилой сектор пуст — но в нём что-то движется.
$outcome$,
  $theory$
`GROUP BY` объединяет строки, относящиеся к одному сектору:

```sql
GROUP BY
  s.sector_id,
  s.sector_code,
  s.sector_name
```

Обычный `COUNT` считает всех назначенных сотрудников:

```sql
COUNT(cs.shift_id)
```

`FILTER` позволяет отдельно посчитать только пропавших:

```sql
COUNT(*) FILTER (
  WHERE cm.official_status = 'missing'
)
```

`WHERE` фильтрует отдельные строки до группировки, а `HAVING` проверяет уже сформированные группы. Сравни количество пропавших с общим количеством сотрудников:

```sql
HAVING COUNT(*) FILTER (
  WHERE cm.official_status = 'missing'
) = COUNT(cs.shift_id)
```

Затем расположи группы по численности от большей к меньшей и оставь первую.
$theory$,
  'Определи жилой сектор, в котором в 2187-09-14 03:40:00 должна находиться отдыхающая смена, но все назначенные сотрудники имеют статус missing. Выведи sector_code, sector_name, общее количество сотрудников как expected_crew и количество пропавших как missing_crew. Если таких секторов несколько, оставь сектор с наибольшим количеством сотрудников.',
  'Соедини crew_shifts, crew_members и sectors. В WHERE оставь жилые сектора, смены rest и заданный момент внутри интервала смены. Сгруппируй результат по сектору, а в HAVING сравни COUNT со счётчиком COUNT(*) FILTER для статуса missing.',
  '[
    {
      "sector_code":"HAB-01",
      "sector_name":"Жилой сектор экипажа",
      "expected_crew":6,
      "missing_crew":6
    }
  ]'::jsonb
),
(
  'prometheus',
  4,
  'Получение последней записи',
  $story$
**Краткая сводка**

- В `HAB-01` Артём столкнулся с мутировавшим человеком в форме технического отдела.
- Настенный сканер распознал биометрическую сигнатуру и связал её с базой экипажа.
- У одного человека может быть несколько сканирований, включая старые нормальные показатели.
- Нужно рассматривать только последнее сканирование каждого члена экипажа.
- Искомый человек числится пропавшим, а уровень тканевой аномалии составляет не менее `80`.
$story$,
  $outcome$
**Первое существо.**

Последняя биометрическая сигнатура принадлежит технику Jason Miller, который должен был отдыхать в `HAB-01` и числился пропавшим.

Сканер не обнаружил пульса или кислорода в крови. Температура тела упала до `29.4`, однако тканевая активность продолжалась, а уровень аномалии достиг `96.7`. Более ранняя запись показывала `62.5`: изменения развивались уже после прекращения нормальных жизненных функций.

Монтажный ключ почти не остановил существо. Артём достаёт из технического шкафа промышленный плазменный резак и повреждает ему конечности, лишая возможности двигаться.

Теперь ясно: экипаж не просто погиб или покинул корабль. Сразу после столкновения внутренний канал связи активируется, и из динамика звучит голос корабельного диспетчера.
$outcome$,
  $theory$
У одного человека может быть несколько строк в `medical_scans`. Чтобы определить время его последнего сканирования, используй `MAX`:

```sql
SELECT MAX(ms2.scanned_at)
FROM medical_scans AS ms2
WHERE ms2.crew_id = ms.crew_id
```

Условие `ms2.crew_id = ms.crew_id` связывает подзапрос с текущей строкой внешнего запроса. Такой подзапрос называется коррелированным: максимальное время вычисляется отдельно для каждого члена экипажа.

Во внешнем запросе оставь строку, время которой совпадает с найденным максимумом:

```sql
ms.scanned_at = (
  SELECT MAX(ms2.scanned_at)
  FROM medical_scans AS ms2
  WHERE ms2.crew_id = ms.crew_id
)
```

После этого можно безопасно проверить сектор, официальный статус и уровень аномалии, не приняв старую запись за текущее состояние человека.
$theory$,
  'Найди члена экипажа, чьё последнее медицинское сканирование выполнено в секторе HAB-01, показывает tissue_anomaly не ниже 80 и принадлежит человеку со статусом missing. Выведи full_name, role, official_status, heart_rate, oxygen_level, body_temperature, tissue_anomaly, medical_status и scanned_at. Оставь только запись с самым высоким уровнем аномалии.',
  'Соедини medical_scans с crew_members и sectors. Сравни ms.scanned_at с SELECT MAX(ms2.scanned_at) в коррелированном подзапросе по crew_id, затем отсортируй по tissue_anomaly DESC и используй LIMIT 1.',
  '[
    {
      "full_name":"Jason Miller",
      "role":"Техник",
      "official_status":"missing",
      "heart_rate":0,
      "oxygen_level":"0.00",
      "body_temperature":"29.40",
      "tissue_anomaly":"96.70",
      "medical_status":"critical",
      "scanned_at":"2187-09-14 03:46:00"
    }
  ]'::jsonb
),
(
  'prometheus',
  5,
  'Сообщения без зарегистрированного отправителя',
  $story$
**Краткая сводка**

- После столкновения в `HAB-01` активировался внутренний аварийный канал.
- Женский голос представился диспетчером и предложил путь к спасательным капсулам через склад.
- В журнале есть сообщения экипажа, автоматики и корабельного ИИ.
- У системных сообщений поле `sender_crew_id` может быть пустым.
- Нужно найти последнее неповреждённое аудиосообщение и проверить зарегистрированного отправителя.
$story$,
  $outcome$
**Голос диспетчера.**

Последнее неповреждённое аудиосообщение связано с Lina Morrow. В базе экипажа она зарегистрирована как диспетчер отдела связи, а сигнатура `VOICE-LM-04` совпадает с её более ранними записями.

На этом этапе журнал не показывает явных признаков подделки: сообщение целое, отправитель существует, а предложенный путь ведёт через доступный технический сектор.

Сразу после передачи дверь жилого сектора автоматически блокируется. Прямой путь к спасательным капсулам закрыт. Единственный доступный маршрут уходит в сторону склада оборудования — теперь Артёму придётся восстановить карту переходов.
$outcome$,
  $theory$
Обычный `JOIN` оставляет только строки, для которых связанная запись найдена в обеих таблицах. Но автоматические и созданные ИИ сообщения могут иметь `NULL` в `sender_crew_id`.

Чтобы такие сообщения не исчезли, используй `LEFT JOIN`:

```sql
LEFT JOIN crew_members AS cm
  ON cm.crew_id = c.sender_crew_id
```

`COALESCE` возвращает первое значение, которое не равно `NULL`:

```sql
COALESCE(cm.full_name, 'Unknown source') AS sender_name
```

Если зарегистрированный член экипажа найден, запрос выведет его имя. Иначе появится `Unknown source`. Аналогично можно получить должность человека или тип системного отправителя:

```sql
COALESCE(cm.role, c.sender_type) AS sender_role
```

После фильтрации канала, типа, времени и признака повреждения отсортируй сообщения от новых к старым и оставь первое.
$theory$,
  'Найди последнее неповреждённое сообщение канала internal_emergency с типом audio, отправленное после 2187-09-14 03:40:00. Выведи message_id, имя отправителя как sender_name, должность или тип отправителя как sender_role, message_text, voice_signature и sent_at. Если член экипажа не найден, вместо имени выведи Unknown source.',
  'Начни с communications AS c и используй LEFT JOIN с crew_members. Для sender_name и sender_role примени COALESCE. В WHERE проверь channel, message_type, is_corrupted = FALSE и время, затем используй ORDER BY c.sent_at DESC и LIMIT 1.',
  '[
    {
      "message_id":517,
      "sender_name":"Lina Morrow",
      "sender_role":"Диспетчер",
      "message_text":"Направляйтесь к спасательным капсулам через склад оборудования. Прямой маршрут закрыт.",
      "voice_signature":"VOICE-LM-04",
      "sent_at":"2187-09-14 03:52:31"
    }
  ]'::jsonb
);

-- Если игрок уже завершил ранее доступные этапы, открываем ему этап 5.
UPDATE quest_progress
   SET current_step = 5,
       completed_at = NULL,
       updated_at = now()
 WHERE quest_slug = 'prometheus'
   AND completed_at IS NOT NULL
   AND current_step < 5;
